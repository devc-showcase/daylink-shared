# 저장소 전체의 배포 버전. 단일 출처다.
#
# 왜 패키지마다 따로 매기지 않나:
#   00 §9가 동등성 검증의 선행 조건으로 "명세 버전 일치"를 요구한다.
#   패키지별 버전이면 네 구현이 어느 시점을 보는지 숫자 여러 개로 따져야 한다.
#   하나면 태그 하나로 끝난다. fixtures만 고쳐도 api-spec 버전이 함께 오르는
#   대가를 치르지만, 추적 가능성이 그보다 값지다.
#
# api-spec/package.json · fixtures/package.json의 version과 반드시 같아야 한다.
# CI가 태그까지 네 값을 대조한다.
sharedVersion=0.2.1
