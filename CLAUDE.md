# CLAUDE.md

`daylink-shared`에서만 쓰는 규칙이다.

> **공통 규칙은 여기 없다.** 용어 표기, 한국어 출력 규칙, 반영 확인 규칙, ALTER 운영은 작업 공간 루트 `portfolio/CLAUDE.md`가 정본이다. 같은 글을 두 곳에 두면 한쪽만 고쳐지므로 옮겨 적지 않는다.
>
> 이 배치에는 알려진 한계가 있다. 루트 파일은 저장소 밖이라 git 이력이 없고, 이 저장소를 단독으로 클론하면 따라오지 않는다. ADR-0013이 규칙도 문서라고 본 취지와 어긋나는 지점이므로, 협업자가 생기면 공통 규칙을 저장소 안으로 되돌린다.

## 이 저장소가 담는 것

명세와 문서의 **정본**이다. 자바 코드는 없다 — `api-spec`은 `openapi.yaml`과 `errors.md`만 담은 패키지로 배포된다(ADR-0015). 소비자는 `daylink-core-api`와 프론트 구현들이다.

문서를 읽는 순서는 [`README.md`](README.md)가 정한다.

## 명세를 고칠 때

**버전은 여섯 곳이 같아야 한다.** 갈라지면 패키지마다 다른 명세가 나간다. 목록과 배포 절차는 [`api-spec/README.md`](api-spec/README.md) §버전이 적히는 곳에 있다.

`openapi.yaml`의 `info.version`을 특히 확인한다. 이 값은 jar와 npm 패키지에 실려 **소비자가 읽는 값**이라, 파일 셋만 맞춰서는 검사되지 않는다. 0.2.0 배포본이 자기를 0.1.0이라고 말한 채 나간 적이 있다.

명세를 고쳤으면 `api-spec`에서 `npm run lint`(redocly)를 돌린다.

## 시드를 고칠 때

`fixtures/`를 고쳤으면 `node validate.mjs`를 돌린다. 검사기가 시드 값을 명세 enum과 대조한다. 상품 상태를 명세에 없는 어휘로 적어 두 번 고친 적이 있다.

## 문서를 고쳤을 때

`node scripts/verify-docs.mjs`를 돌린다. 문서끼리 어긋난 곳을 찾는다 — 상위 문서 버전 참조, 패키지 스코프 잔재, 화면 수 표기, ADR 상태표와 파일 머리말의 불일치.

`validate.yml`이 보는 것(명세 문법·타입 생성·시드 무결성·버전 네 곳)은 검사하지 않는다. 겹치지 않게 나눠 두었다.

ADR 디렉터리는 내용 검사에서 빠진다. 결정 시점의 기록이므로 옛 이름과 옛 수치가 남아 있는 것이 정상이다.

## ADR을 쓸 때

절차는 [`docs/adr/README.md`](docs/adr/README.md) §쓰는 법에 있다. 두 가지만 여기 옮긴다.

- **근거에는 탈락한 대안과 그 이유를 반드시 적는다.** 이 저장소의 ADR이 대화 맥락 없이도 복원되는 이유가 이것이다
- **이미 쓴 ADR은 고치지 않는다.** 생각이 바뀌면 새 ADR을 쓰고 옛 ADR의 상태만 `대체됨(→ ADR-NNNN)`으로 바꾼다

## 값을 새로 정할 때

이 저장소의 PRD에는 **근거 없이 확정된 수치가 여럿 있다** — 수수료 12%, 홀드 10분, 인원 상한 6, 스냅숏 2시간, 동시 요청 N=50, 취소 정책 72/24h·100/50/0%. 값을 정한 대화는 남아 있지 않다.

그러므로 새 수치를 정할 때는 **그 값을 고른 이유를 같은 자리에 한 문장으로 적는다.** 기술 판단이면 ADR로, 제품 정책이면 해당 PRD 절에 적는다. "학습용 가정"이라고만 쓰면 나중에 무엇을 재검토해야 할지 알 수 없다.
