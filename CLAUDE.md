# CLAUDE.md

이 저장소에서 작업할 때 지켜야 할 규칙.

> **위치.** 이 파일은 `daylink-shared/` 안에 둔다. 규칙도 문서이므로 git이 버전을 남겨야 한다([ADR-0013](docs/adr/0013-documents-live-in-repo.md)). 저장소 밖(`portfolio/` 루트)에 두면 이력도 백업도 없다.
>
> `daylink-core-api` 같은 형제 저장소가 생기면 여섯 저장소 공통 규칙만 따로 빼야 한다. 그때 무엇이 공통인지가 분명해지므로 지금은 나누지 않는다.

## 1. 용어 표기

**국내 개발자 커뮤니티와 기술 블로그에서 실제로 통용되는 형태를 쓴다.**

- 애매하면 **한글 음차**를 택한다 — 커밋, 폴백, 마운트, 롤백, 캐시, 배포, 스냅숏
- **국립국어원식 순화어는 쓰지 않는다.** 억지 번역(되돌리기, 예비 동작, 붙이기 같은 표현)은 오히려 읽는 사람을 멈추게 한다
- 영어 병기는 **처음 한 번만** 괄호로 하고, 그다음부터는 병기하지 않는다

```
✅ 멱등성(idempotency)을 보장한다. 멱등 키는 헤더로 받는다.
❌ 멱등성(idempotency)을 보장한다. 멱등 키(idempotency key)는 헤더(header)로 받는다.
```

기준 독자는 **국내 웹 프론트엔드 실무자**다. JavaScript·TypeScript를 주력으로 쓰고 SI 현장에서 일하는 개발자가 읽는다고 보고 쓴다.

## 2. 한국어 출력 규칙

### 2.1 처음부터 한국어로 쓴다

영어로 답을 만든 뒤 번역하지 않는다. 번역체는 문장 구조에서 드러난다.

### 2.2 용어는 §1을 따른다

### 2.3 금지 표현

다음은 쓰지 않는다.

| 금지 | 대신 |
|---|---|
| ~를 통해 | ~로, ~에서, ~가 (조사로 붙인다) |
| ~에 대해 | ~를, ~의 (목적어로 바꾼다) |
| ~에 있어서 | ~에서, ~일 때 |
| ~되어진다 | ~된다 |
| 가지고 있다 | 있다, ~다 |
| ~할 필요가 있다 | ~해야 한다 |
| 결론적으로 | (빼거나 결론을 그냥 쓴다) |
| 주목할 만하다 | (빼거나 왜 중요한지 쓴다) |

### 2.4 무생물을 주어로 쓰지 않는다

```
✅ 이 함수가 값을 돌려준다
❌ 이 함수는 값을 제공합니다
```

### 2.5 관형절을 두 개 이상 쌓지 않는다

명사 앞에 `~하는`, `~된`, `~인` 수식을 겹치지 않는다. 길어지면 문장을 끊는다.

```
✅ 만료된 hold를 조회했다. 이때 서버가 돌려주는 값이 문제다.
❌ 만료된 hold를 조회했을 때 서버가 돌려주는 값에 대한 판단
```

### 2.6 문장 길이를 섞는다

한 문단 안에서 긴 문장과 짧은 문장을 섞는다. 짧은 문장이 리듬을 만든다.

### 2.7 모르면 모른다고 쓴다

확신이 없을 때 완곡하게 돌리지 않는다. "~일 수도 있습니다"로 뭉개는 대신 "모르겠다", "확인이 필요하다"라고 쓴다. 근거가 없는 추측을 사실처럼 쓰지 않는다.

## 3. 반영 확인 — 고쳤다고 쓰기 전에 파일을 연다

**다른 문서를 고쳤다고 쓸 때는 그 파일을 실제로 열어 확인한 뒤에 쓴다.** 기억·의도·계획으로 쓰지 않는다.

판단을 적는 곳과 반영하는 곳이 다르기 때문에 생기는 문제다. 체크리스트에 "`02`를 v2.2로 정정했다"고 적는 순간 그 항목은 닫히고, 실제로 안 고쳤어도 아무도 다시 안 본다. **틀린 기록은 빈칸보다 나쁘다.**

2026-08-31에 체크리스트가 네 가지를 "고쳤다"고 단언했는데 넷 다 파일에 없었다 — `02` v2.2 정원 규칙, `05 §5.2`, ADR-0014, `00 §5.1`.

### 지킬 것

- 완료·정정·반영·신설을 적기 전에 **대상 파일을 읽고 그 문장을 찾는다**
- 버전을 올렸다고 적으면 머리말 버전 줄을 확인한다. 새 파일을 만들었다고 적으면 목록에서 확인한다
- 확인 못 했으면 **"고쳤다"가 아니라 "고쳐야 한다"로 적는다**
- 판단 기록과 그 반영을 같은 커밋에 넣는다

### Claude가 할 것

문서 작업을 마칠 때마다 **완료 주장을 기계로 대조하고 결과를 보고한다.** 사용자가 매번 요청하지 않아도 한다.

1. `정정했다`·`반영했다`·`해소했다`·`고쳤다`·`신설했다`·`추가했다`·`갱신했다` 같은 완료 주장을 저장소 전체에서 훑는다
2. 검증 가능한 주장(파일 존재, 문자열 포함, 버전 값, ID·경로·상수)은 스크립트로 확인한다
3. 반영 안 된 것을 **개수와 함께** 보고한다. 조용히 고치고 넘어가지 않는다

**파일 상태를 판단하기 전에 반드시 재스테이징하고 mtime을 확인한다.** `/mnt/user-data/uploads/`의 사본은 자동으로 갱신되지 않는다. 낡은 사본을 읽고 "미반영"이라고 잘못 보고한 적이 있다.

---

적용 대상은 저장소의 모든 문서(PRD·ADR·체크리스트·README)와 대화 응답 모두다.
